using System.Security.Cryptography;

namespace leetcode_1;

class Program
{
    static void Main(string[] args)
    {
       Console.WriteLine("Anagram");
       Console.WriteLine("Now enter S :");
       String s = new String(Convert.ToString(Console.ReadLine()));
       Console.WriteLine("Now enter S :");
       String t = new String(Convert.ToString(Console.ReadLine()));
       bool answer = false;
        if (s.Length == t.Length){

        }



       Console.WriteLine(answer.ToString()+s.Length);

    }
}

